import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        marginTop: 50,
        alignItems: "center",
      },
      titlelogo: {
        width: 100,
        height: 100,
      },
      titletext: {
        fontSize: 30,
        fontWeight: "bold",
        paddingLeft: 10,
      },
})

export default styles